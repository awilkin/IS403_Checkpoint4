@model IEnumerable<BlowOut.Models.Instrument>

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New Instrument", "Create")
    </p>
   <!-- <div class="container">
            <div>
                @Html.DisplayNameFor(model => model.instName)
            </div>
            <div>
                @Html.DisplayNameFor(model => model.Desc)
            </div>
            <div>
                @Html.DisplayNameFor(model => model.NewPrice)
            </div>
            <div>
                @Html.DisplayNameFor(model => model.UsedPrice)
            </div>
            <div>
                @Html.DisplayNameFor(model => model.condition)
            </div>
            <div>
                @Html.DisplayNameFor(model => model.ClientID)
            </div>
        <div></div>
        </div>
    -->
        @foreach (var item in Model)
        {
            <div class="container">
                <a href='@Url.Action("Instrument", "Home", new { id = item.InstrumentID })' >
                <div class="row" style="border: 1px; border-color: lightgray;">
                    <div class="col-xs-9">
                        <div class="InstaHead">
                            @Html.DisplayFor(modelItem => item.instName)
                        </div>
                        <div>
                            @Html.DisplayFor(modelItem => item.Desc)
                        </div>
                        <div>
                            <strong>New Price: </strong>$@Html.DisplayFor(modelItem => item.NewPrice)
                        </div>
                        <div>
                            <strong> Used Price: </strong>$@Html.DisplayFor(modelItem => item.UsedPrice)
                        </div>
                        <div>
                            @Html.DisplayFor(modelItem => item.condition)
                        </div>
                        <div>
                            @Html.DisplayFor(modelItem => item.ClientID)
                        </div>
                        <div>
                            @Html.ActionLink("Edit", "Edit", new { id = item.InstrumentID }) |
                            @Html.ActionLink("Details", "Details", new { id = item.InstrumentID }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.InstrumentID })
                        </div>
                    </div>
                    <div class="col-xs-3">
                        <div class="thumbnail">
                            <img src="~/Content/images/@Html.DisplayFor(modelItem => item.pic)" alt="@Html.DisplayFor(modelItem => item.instName)" />
                        </div>
                    </div>
                </div>
                </a>
                <div class="space"></div>
            </div>
        }
</body>
</html>
<!--  -->